import React, {useState, useEffect} from 'react';
import { observable } from 'mobx-react';

const AddUserAction = observable((props) => {

    return (
        <div>AddUserAction</div>
    )
})

export default AddUserAction;